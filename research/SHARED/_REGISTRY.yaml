# SHARED/_REGISTRY.yaml — Index of all shared data files across niches
#
# Created: 2026-07-23 (Workstream C, SRE-M07 fix)
# Bootstrap: populated during first 5 niches.
# After niche 5, the registry should cover ~60% of common cross-niche data.
# Bootstrap health check runs at niche 5: if registry >50% empty, escalate.

schema_version: 1.0
last_updated: '2026-07-23'
niche_count_at_last_update: 0

competitor_profiles: []     # {competitor_id, name, niche_id, file_path, fetch_date, fresh_until}
benchmarks: []              # {benchmark_id, metric, niche_id, file_path, fetch_date}
trigger_registries: []      # {trigger_id, name, niche_id, file_path, fetch_date}
shared_tools: []            # {tool_name, capability, file_path}
regulatory_items: []        # {regulation_id, name, file_path}
taxonomy_entries: []        # {category, niche_type, file_path}
